@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model IEnumerable<Models.Customer>

<h1>Customer List</h1>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-striped table-hover table-responsive" >
    <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Address</th>
        <th>Email</th>
        <th>Phone</th>
    </tr>

    @foreach(var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(i => item.CustNumber)</td>
                    <td>@Html.DisplayFor(i => item.CustName)</td>
                    <td>@Html.DisplayFor(i => item.CustAddress)</td>
                    <td>@Html.DisplayFor(i => item.CustEmail)</td>
                    <td>@Html.DisplayFor(i => item.CustPhone) </td>
                    <td>
                        @Html.ActionLink("Details", "Details", new { id = item.CustNumber }) |
                        @Html.ActionLink("Edit", "Edit", new { id = item.CustNumber }) |
                        @Html.ActionLink("Delete", "Delete", new {id = item.CustNumber})
                    </td>
                </tr>
            }
</table>
